{
  "name": "appliance-error-codes",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "seed": "cd ../data-seed && cargo run --release -- --out ../appliance-site/src/data/error-codes.json",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "build:local": "npm run seed && astro build"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.16.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260613.1"
  }
}
